apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: build-scratch-volume-claim
spec:
  storageClassName: sdf-group-ad
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi